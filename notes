Instructions for server:

Look up how to create an ssh key and connect it to digitalocean

ssh -X root@138.197.215.196

OR

you can acccess the server under access tab under our droplet on digital ocean with username: root and password: encoreradio

Webpack Command:
webpack -w

Nodemon Command:
nodemon app


TO DOWNLOAD AND CONVERT TO AUDIO FILE:

var audio = require('./getAudio.js');
audio.getAudio(url);

//This stores the song as "<videoID>.mp4" where <videoID> = url.substring(24);
//Song mp4's are stored in /Encore-Radio/songs
